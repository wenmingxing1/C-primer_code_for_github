#include<iostream>
#include<vector>
#include<string>
#include<deque>
#include<list>

using namespace std;

int main()
{
    list<Sales_data>
    deque<double>

    //假定noDefault是一个没有默认构造函数的类型
    vector<noDefault> v1(10, init);     //正确，提供了元素初始化器init
    vector<noDefault> v2(10);       //错误，必须提供一个元素初始化器

    //iter是通过list<string>定义一个迭代器类型
    list<string>::iterator iter;
    //count是通过vector<int>定义的一个difference_type类型
    vector<int>::difference_type count;
