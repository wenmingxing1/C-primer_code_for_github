#include<iostream>
#include<stdexcept>

using namespace std;

int main()
{
    if(item1.isbn( != item2.isbn()))
        throw runtime_error("Data must refer to same ISBN");
    cout << item1 +item2 << endl;
}
